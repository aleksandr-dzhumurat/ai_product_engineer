# Direct dependencies for src/scraping's scripts. Install with:
#   uv venv
#   uv pip install --python .venv/bin/python -r requirements.txt
#
# Only directly-imported packages are listed - transitive dependencies
# (e.g. requests' urllib3/certifi/idna, cloakbrowser's playwright/httpx)
# are resolved automatically by uv.

beautifulsoup4          # bs4 - HTML parsing: blog_rss_scrapers.py, blog_feed_scrapers.py, scrape_nebius.py, scrape_tavily.py
requests                # HTTP fetching: blog_rss_scrapers.py, scrape_nebius.py, scrape_tavily.py
feedparser              # RSS/Atom parsing: blog_rss_scrapers.py
python-dateutil         # fuzzy date parsing: blog_rss_scrapers.py
cloakbrowser            # stealth browser automation for JS-rendered blogs: browser_use.py
opentelemetry-sdk                      # Grafana Cloud metrics: metrics.py
opentelemetry-exporter-otlp-proto-http # OTLP export transport: metrics.py
openai                  # Nebius (OpenAI-compatible) client: llm_client.py
backoff                 # retry/backoff decorator: llm_client.py
python-dotenv           # loads src/scraping/.env: llm_client.py, tg_pusher.py
